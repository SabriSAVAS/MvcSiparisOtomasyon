@if (TempData["_Alerts"] != null)
{

}


@using SiparisEnt.WebUI.Infrastructure.Alerts
@if (TempData.GetAlerts().Any())
{
    <script type="text/javascript">
       

        $(function() {
            @foreach (var alert in TempData.GetAlerts())
            {
                @*<text>
                    noty({ text: '@alert.Message', layout: 'topRight', type: '@alert.AlertClass' });
                </text>*@
               
                <text>

                     var command = "@alert.AlertClass";

                     var message = "@alert.Message.Replace("'", "\\'")";
                     // toastr["success"]("My name is Inigo Montoya. You killed my father. Prepare to die!")
                     toastr["@alert.AlertClass"]("@alert.Message.Replace("'", "\\'")")

                 </text>

            }
        });
    </script>
}
@*<text>
        window.alerts.showAlert({
        message: '@alert.Message.Replace("'", "\\'")',
        alertClass: '@alert.AlertClass'
        });
    </text>*@ @*<text>
        <div class="alert alert-@alert.AlertClass alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <strong>@alert.AlertClass.ToString() </strong>@alert.Message
        </div>
    </text>*@


@*<text>
    noty({ text: '@alert.Message', layout: 'topRight', type: '@alert.AlertClass' });
</text>*@